defmodule DesignPattern.Bridge.Example do
  @moduledoc """
    【桥接模式】的定义
    是一种结构型设计模式，
    可将业务逻辑或一个大类拆分为不同的层次结构，
    从而能独立地进行开发。
  """
  @doc """
    【个人理解】
    将一个功能中不同维度的变化独立封装起来，
    在通过组合的方式将不同维度进行关联，
    而函数式语言中“模块”的特性本身就支持这一点，
    可以在一个模块中直接使用另一个模块中的方法
  """
end
